; generated by drom from package skeleton 'library'

(library
  (name superbol_free_lib)
  (public_name superbol_free_lib)
  (wrapped true)
  ; use field 'dune-libraries' to add libraries without opam deps
  (libraries vscode-json superbol_preprocs sql_preproc lwt ez_toml ez_file ez_cmdliner ez_api.encoding cobol_typeck cobol_parser cobol_lsp cobol_indent cobol_common )
  ; use field 'dune-flags' to set this value
  (flags (:standard))
  ; use field 'dune-stanzas' to add more stanzas here
  (preprocess (pps ppx_cobcflags ppx_deriving_encoding))
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package superbol_free_lib))

; use field 'dune-trailer' to add more stuff here

