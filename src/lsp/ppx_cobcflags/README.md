# Ppx_cobcflags package

This package contains ppx rewriters to generate the cobc cli flags in `superbol`. To see which
rewriters are provided and what they are generating, please look at the package documentation which
gives a few use cases.
