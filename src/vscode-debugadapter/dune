; generated by drom from package skeleton 'js_library'

(library
  (name debugAdapter)
  (public_name vscode-debugadapter)
  (wrapped true)
  (libraries vscode-debugprotocol promise_jsoo node-js-stubs jsonoo js_of_ocaml interop-js-stubs gen_js_api )
  (modes byte)
  (preprocess (pps gen_js_api.ppx))
    (js_of_ocaml (javascript_files vscode_debugadapter_stub.js))

  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package vscode-debugadapter))


