; generated by drom from package skeleton 'js_library'

(library
  (name vscode_languageclient)
  (public_name vscode-languageclient)
  (wrapped true)
  (libraries vscode promise_jsoo jsonoo js_of_ocaml interop gen_js_api )
  (modes byte)
  (preprocess (pps gen_js_api.ppx js_of_ocaml-ppx))
  (js_of_ocaml
  (javascript_files vscode_languageclient_stub.js))

  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package vscode-languageclient))


