; generated by drom from package skeleton 'js_library'

(library
  (name polka_js_stubs)
  (public_name polka-js-stubs)
  (wrapped true)
  (libraries promise_jsoo node-js-stubs jsonoo js_of_ocaml interop-js-stubs gen_js_api )
  (modes byte)
  (preprocess (pps gen_js_api.ppx))
   (js_of_ocaml (javascript_files polka_stub.js))

  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package polka-js-stubs))


