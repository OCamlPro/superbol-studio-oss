; generated by drom from package skeleton 'js_library'

(library
  (name superbol_vscode_lib)
  (public_name superbol-vscode-lib)
  (wrapped true)
  (libraries vscode-languageclient-js-stubs vscode-js-stubs promise_jsoo node-js-stubs jsonoo js_of_ocaml )
  (modes byte)
  (preprocess (pps gen_js_api.ppx))
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} -I +unix unix.cma %{script}))))

(documentation
  (package superbol-vscode-lib))


