; generated by drom from package skeleton 'js_library'

(library
  (name superbol_debugger)
  (public_name superbol-debugger)
  (wrapped true)
  (libraries vscode-js-stubs vscode-debugprotocol vscode-debugadapter superbol-vscode-lib promise_jsoo node-js-stubs js_of_ocaml str)
  (modes byte)
  (preprocess (pps gen_js_api.ppx))
    (enabled_if (= %{context_name} "default"))

  )

(ocamllex mi2Lexer)
(menhir (modules mi2Parser))

(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} -I +unix unix.cma %{script}))))

(documentation
  (package superbol-debugger))


