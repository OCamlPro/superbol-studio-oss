# Tiny abstraction layer on top of `libcob-ctypes`
 
This library introduces some helper types, exceptions, and functions
with labeled or optional arguments on top of the raw bindings exposed
by `libcob-ctypes`.
