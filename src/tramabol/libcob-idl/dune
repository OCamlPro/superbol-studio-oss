; generated by drom from package skeleton 'library'

(library
  (name libcob_idl)
  (public_name libcob-idl)
  (wrapped true)
  ; use field 'dune-libraries' to add libraries without opam deps
  (libraries  camlidl)
  ; use field 'dune-flags' to set this value
  (flags (:standard))
  ; use field 'dune-stanzas' to add more stanzas here
  
  (foreign_archives ../cobaux)
(foreign_stubs (language c)
               (names module_stubs field_stubs termio_stubs cobaux_caml)
 (flags :standard -Wno-discarded-qualifiers)
 (include_dirs (lib camlidl)
               %{env:GNUCOBOL_INCLUDE=/usr/local/include}
               "../cobaux"))
(c_library_flags :standard
                 -L%{env:GNUCOBOL_LIBDIR=/usr/local/lib} -lcob)

  )


(documentation
  (package libcob-idl))

; use field 'dune-trailer' to add more stuff here
(rule
 (targets module.ml module.mli module_stubs.c module.h
          field.ml field.mli field_stubs.c field.h
          termio.ml termio.mli termio_stubs.c termio.h)
 (deps (glob_files *.idl))
 (action (run camlidl -header %{deps})))

