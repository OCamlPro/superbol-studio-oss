; generated by drom from package skeleton 'library'

(library
  (name libcob_ctypes)
  (public_name libcob-ctypes)
  (wrapped true)
  ; use field 'dune-libraries' to add libraries without opam deps
  (libraries  )
  ; use field 'dune-flags' to set this value
  (flags (:standard))
  ; use field 'dune-stanzas' to add more stanzas here
  
   (foreign_archives ../cobaux)
 (ctypes
  (external_library_name libcob)
  (build_flags_resolver
   (vendored
    (c_flags "-I%{env:GNUCOBOL_INCLUDE=/usr/local/include}"
             "-include" "../cobaux/cobaux.h"
             :standard)
    (c_library_flags "-L%{env:GNUCOBOL_LIBDIR=/usr/local/lib}"
                     "-lcob"
                     ; "-lcob -lgmp -lreadline -lxml2 -lcjson -lncursesw -ltinfo -lpanelw"
                     :standard)))
  (headers (include "libcob.h"))
  (type_description
   (instance Types)
   (functor Libcob_types))
  (function_description
   (concurrency unlocked)
   (instance Module)
   (functor Libcob_module))
  (function_description
   (concurrency unlocked)
   (instance Field)
   (functor Libcob_field))
  (function_description
   (concurrency unlocked)
   (instance Termio)
   (functor Libcob_termio))
  (generated_types Libcob_types_generated)
  (generated_entry_point V1))

  )


(documentation
  (package libcob-ctypes))

; use field 'dune-trailer' to add more stuff here

